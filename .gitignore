# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual env / vendored libs (7.3G / 193M — never commit)
.venv/
.pylibs/

# Claude local dir — settings and skills (not part of the published repo)
.claude/

# Run outputs / results (regenerated by train/render/eval)
runs/

# Data & model artifacts — real data lives on /lustre, never in-repo
*.npy
*.npz
*.pth
*.pt
*.h5

# Local figures — do NOT push (regenerable). Keep only the remote's motivation.png.
figures/*
!figures/motivation.png

# OS
.DS_Store
